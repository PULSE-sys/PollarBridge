{
  "name": "pollar-bridge-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"blue,cyan\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"blue,cyan\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
